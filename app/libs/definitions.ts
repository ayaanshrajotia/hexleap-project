export type PlayerCardProps = {
    playerName: string;
    playerImage: string;
    totalEvents: number;
    sports: string;
};

export type AdCardProps = {
    adImage: string;
    adTitle: string;
    adDesc: string;
};

export type SpotlightCardProps = {
    title: string;
    image: string;
    date: string;
    day: string;
    time: string;
    address: string;
    collection: string;
};
